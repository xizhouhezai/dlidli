# tasks：弹幕系统

> 对应规格：[spec](/specs/danmaku/spec) ｜ 方案：[plan](/specs/danmaku/plan)
> 任务编号：`{阶段}-{模块}-{序号}`；完成即勾选并追加完成日期；括号补注实现要点与验证结论。

## M1（W5-W12）

- [x] M1-DM-01 后端：弹幕发送（Lv1 门槛 + 5s 频控 + 敏感词影子屏蔽；机审词库待 M2-AUD） `2026-07-29`
  - 覆盖：DM-01、DM-04（5s 频控）、DM-24
- [x] M1-DM-02 后端：分段拉取接口（6min/段）+ Redis 段缓存（写后失效） `2026-07-29`
  - 覆盖：DM-10
- [x] M1-DM-03 弹幕渲染引擎（M1 版：DOM 轨道分配渲染，滚动/顶部/底部三模式；Canvas 高性能版随 packages/player 重构） `2026-07-29`
  - 覆盖：DM-10、DM-03
- [x] M1-DM-04 Web：弹幕输入框/开关/乐观上屏（不透明度等展示设置延至 V1 弹幕进阶） `2026-07-29`
  - 覆盖：DM-11

## M2（W13-W24）

> 2026-08-03 需求升级（对标 B 站，详见 [spec](/specs/danmaku/spec) changelog V1.1）：新增列表面板/悬停操作/展示设置/服务端屏蔽/实时 WS；弹幕点赞延后（复用 user_action obj_type=3）；UP 主弹幕管理（DM-23）延后。

- [x] M2-DM-01 发送端 UI：输入框工具条（滚动/顶部/底部模式选择 + 8 预设色板，非白/顶底 Lv3 解锁置灰提示）；重复内容 30s 去重（后端 Redis，40004）；弹幕列表面板（独立弹窗：时间排序全量列表分页 + 点击跳转进度） `2026-08-03`
  - 覆盖：DM-02、DM-03、DM-04（重复去重）、DM-16
- [x] M2-DM-02 屏蔽与展示设置：0014 迁移 danmaku_block 表（服务端账号级：关键词 ≤200 + 用户按 UID 哈希/直传哈希）+ CRUD 接口 + 拉取接口登录态过滤；设置页弹幕屏蔽卡片（关键词增删 + 已屏蔽用户列表）；弹幕设置面板（不透明度/字号缩放/显示区域 1/4·半屏·全屏/滚动速度/同屏密度，localStorage 持久化） `2026-08-03`
  - 覆盖：DM-20、DM-21、DM-12、DM-13
- [x] M2-DM-03 WebSocket 实时弹幕：/api/v1/videos/:bvid/danmaku/ws 视频房间广播（gorilla/websocket，发送成功即时推送同视频在线连接，Origin 白名单防劫持，单房间 5000 连接上限）+ 前端断线重连（5 次指数退避）+ WS 不可用回退 HTTP 分段拉取；Vite 代理 ws:true；双连接广播实测通过 `2026-08-03`
  - 覆盖：DM-15
- [x] M2-DM-04 弹幕举报入口：悬停操作（暂停滚动 + 复制/举报/屏蔽发送者）；后端与后台处理队列已随 M2-AUD-03 完成（弹幕类型 + admin 删除/处罚） `2026-08-03`
  - 覆盖：DM-17、DM-22

## 进度

| 里程碑 | 任务数 | 已完成 |
| --- | :-: | :-: |
| M1 | 4 | 4 |
| M2 | 4 | 4 |
| **合计** | **8** | **8** |

> 勾选任务后同步更新上表与 [开发进度管理](/project/progress) 的模块矩阵。
