# plan：`{模块名}`

> 对应规格：[spec](/specs/templates/spec) ｜ 技术基线：[后端架构](/architecture/backend) · [数据模型](/architecture/data-model) · [前端架构](/architecture/frontend)
> plan 只写模块级方案与差异；全局分层/规范/部署以架构文档为准。

## 1. 方案概览

（一段话说清本模块的技术方案全貌：核心链路、所属模块边界、关键组件）

## 2. 技术决策

| 决策点 | 决策 | 理由 | 备选方案 |
| --- | --- | --- | --- |
| `{决策点}` | `{采用方案}` | `{为什么}` | `{被否掉的方案及否因}` |

## 3. 数据模型

（模块相关表结构/Redis Key；字段注释写枚举含义。全局分表/ID 策略见 [数据模型](/architecture/data-model)，此处只列模块私有部分与差异。占位符用反引号包裹，禁止裸写花括号）

```sql
-- 示例：模块私有表
```

## 4. 接口设计

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/v1/...` | ... |

> 接口事实文档以 swaggo 生成的 OpenAPI 为准（`server/docs/`，本地 `http://localhost:8000/swagger/index.html`），此处登记模块级清单与约定。

## 5. 关键流程

（状态机、时序、算法等实现要点；业务规则属于 spec，此处只写实现）

```
（流程图/伪代码）
```

## 6. 风险与待定项

- [ ] `{已知风险 / 未决技术问题 / 规模化演进项}`
