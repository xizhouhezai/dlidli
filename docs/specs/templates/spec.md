# spec：`{模块名}`

> 模块标识：`{module}` ｜ 优先级：P0/P1/P2 ｜ 覆盖版本：MVP / V1 / V2 / V3
> 上游输入：[产品概述](/product/overview) · [用户画像](/product/personas) · [版本规划](/product/versions) · [非功能需求](/product/nfr)（全局约束，默认继承）
> 实施状态：见 [tasks](/specs/templates/tasks)（spec 不记录进度）
>
> 变更记录：
> - 2026-xx-xx V1.0 初稿

## 1. 背景与用户故事

（1-3 句说明为什么做这个模块；2-4 条 User Story：作为 X，我可以 Y，以便 Z）

## 2. 需求规格

> 验收标准使用 EARS 语法（见 [SDD 总纲](/specs/)），关键字粗体。数量约束（字数/上限/时限等）写进语句或以括号标注。
> 占位符一律用反引号包裹（如 `{触发}`），**禁止裸写花括号**——VitePress 构建会把裸花括号当 Vue 模板语法解析导致构建失败。

### 2.1 `{子域}`（`{版本}`）

| ID | 需求 | 验收标准（EARS） | 优先级 |
| --- | --- | --- | --- |
| `{MOD}`-01 | `{一句话需求名}` | **WHEN** `{触发}` **THE SYSTEM SHALL** `{响应}` | P0 |

### 2.2 `{子域}`（`{版本}`）

（同上；子域之间用二级标题分段，版本标注在标题中）

## 3. 边界与异常

- **IF** `{异常条件}` **THEN THE SYSTEM SHALL** `{处置方式}`
- **WHEN** `{触发}` **THE SYSTEM SHALL NOT** `{禁止行为}`（可选，仅用于强禁止）

## 4. 成功指标

| 指标 | 目标 |
| --- | --- |
| `{可量化指标}` | `{阈值}` |

## 5. 依赖与关联

- 依赖：`{上游能力，链接到对应 spec 与需求 ID}`
- 被依赖：`{哪些模块消费本模块，链接 + ID}`
- 全局约束：[非功能需求](/product/nfr) 中与本模块强相关的条目（如安全/合规）
