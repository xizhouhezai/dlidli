# tasks：搜索与推荐

> 对应规格：[spec](/specs/search-recommend/spec) ｜ 方案：[plan](/specs/search-recommend/plan)
> 任务编号：`{阶段}-{模块}-{序号}`；完成即勾选并追加完成日期；括号补注实现要点与验证结论。

## M2（W13-W24）

- [ ] M2-SRH-01 基建：Elasticsearch 部署 + IK 分词 + 索引设计（本地无 Docker，MVP 已用 MySQL LIKE 替代，接口层可平滑切换）
  - 覆盖：SRH-01、SRH-02（基建前置）
- [ ] M2-SRH-02 后端：稿件索引同步（Kafka 消费者）
  - 覆盖：SRH-01（索引时效）
- [x] M2-SRH-03 后端：搜索接口（视频标题/UP 主昵称，MySQL LIKE + 分页；排序/筛选/联想/热搜随 ES 接入补） `2026-07-29`
  - 覆盖：SRH-01（MVP 形态）
- [x] M2-SRH-04 Web：搜索页（视频/用户 Tab + 分页；头部搜索框接真；筛选器/历史/联想下拉待补） `2026-07-29`
  - 覆盖：SRH-01（前端）

## M3（W25-W48）

> 2026-08-04 落地（本地务实版）：行为日志 MySQL 落库（ClickHouse 预留）；ItemCF 初期以「兴趣分区 + 热度」替代，2026-08-05 已由 cmd/itemsim 真实 ItemCF 补齐；向量/离线计算待规模化。

- [x] M3-REC-01 行为日志采集管道（曝光/点击/播放/互动 → ClickHouse）（0016 迁移 user_behavior 表本地落库（ClickHouse 预留）；POST /behaviors 批量上报（1曝光 2点击 3播放 4互动，ID 字符串化）；首页推荐 Tab 曝光上报） `2026-08-04`
  - 覆盖：REC-03、REC-12（数据基建）
- [x] M3-REC-02 热度榜计算任务（全站/分区，定时）（加权分：播放×1+赞×3+币×5+藏×4+评×4+弹幕×2+转发×3；GET /videos/hot 全站/分区榜，Redis 5min 缓存） `2026-08-04`
  - 覆盖：REC-03
- [x] M3-REC-03 ItemCF 离线计算 + 相似视频存储（0019 迁移 video_similar（video_id+similar_video_id 唯一+score）；cmd/itemsim 离线计算：user_behavior action=3 构造用户-物品矩阵 → 共现 → 余弦相似度（共现数/sqrt(|Ua||Ub|)）→ 每稿 top 10（阈值 0.1）upsert 幂等；推荐召回接入：个性化时取最近观看 2 视频的相似视频（DISTINCT 限流 12）加入候选池；E2E 实测：3 用户 5 视频共现 → 14 条相似关系（0.82/0.71/0.58）→ 推荐列表出现相似视频） `2026-08-05`
  - 覆盖：REC-01（个性化召回）
- [x] M3-REC-04 推荐服务（召回合并/规则粗排/多样性打散/已看过滤）（GET /recommend/videos：混合召回（兴趣分区热度 + 全站热度 + 新稿池）+ 已看过滤（行为日志播放记录）+ 负反馈过滤（内容/UP主/分区）+ 打散（同 UP 一屏≤1、同分区连续≤3）） `2026-08-04`
  - 覆盖：REC-01、REC-10、REC-11、REC-12
- [x] M3-REC-05 首页信息流接入 + 负反馈（不感兴趣）（首页新增「推荐」Tab（默认），卡片 hover「不感兴趣」按钮（POST /dislikes，本地移除+后端过滤）；设置页「个性化推荐」开关） `2026-08-04`
  - 覆盖：REC-01、REC-11
- [x] M3-REC-06 新稿冷启动扶持池（发布 48h 内播放 <100 的稿件保底混入推荐池） `2026-08-04`
  - 覆盖：REC-13
- [x] M3-REC-07 关闭个性化推荐开关（合规）（user.recommend_on 字段 + GET/PUT /users/me/recommend-settings；关闭后退化为热度榜；设置页开关卡片） `2026-08-04`
  - 覆盖：REC-14

## 进度

| 里程碑 | 任务数 | 已完成 |
| --- | :-: | :-: |
| M2 | 4 | 2 |
| M3 | 7 | 7 |
| **合计** | **11** | **9** |

> 勾选任务后同步更新上表与 [开发进度管理](/project/progress) 的模块矩阵。
