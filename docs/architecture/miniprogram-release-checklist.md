# 微信小程序发布验收清单（M3-MP-03）

> **当前口径（2026-09-15）**：本项目为**个人主体**小程序（AppID `wx5a8b4b33acc3ec20`），
> 个人主体**无法申请视频类目**，因此**不提交平台审核**，只做本地测试验收。
> 本清单保留完整的发布步骤，供将来主体升级为企业后启用。

## 主体与资质前提

- [x] 确认为个人主体小程序，AppID `wx5a8b4b33acc3ec20`。
- [ ] ~~申请并通过微信视频号/短视频相关类目资质~~ —— **个人主体不具备申请资格，明确不做**。
      （企业主体升级后才恢复此项；在此之前不对外分发小程序，仅本地测试。）

## 发布前配置

- [x] 真实 AppID 已写入 `apps/h5/src/manifest.json` 的 `mp-weixin.appid`
      （构建产物 `project.config.json` 已带该 AppID）。
- [x] AppID 不提交到不应暴露的环境配置中；Secret 通过部署环境变量注入。
- [ ] 后端通过部署密钥注入 `DLIDLI_WECHAT_MPAPPID` 与 `DLIDLI_WECHAT_MPAPPSECRET`；
      Secret 禁止写入仓库、日志和前端产物。（本地可用开发工具联调）
- [ ] 在微信公众平台配置服务器域名（API、静态资源、视频播放），并完成 HTTPS 证书校验。
      （本地测试可在开发者工具勾选"不校验合法域名"，故非本地阻塞项）
- [x] 确认后端部署环境能访问微信 `jscode2session` 接口（`wechat.Code2Session` 已接入）。

## 构建与导入

```bash
pnpm mp:build
```

- [x] 构建成功，可导入 `apps/h5/dist/build/mp-weixin`。
- [x] 主包大小保持在 1.5MB 目标以内（实测**约 203KB**）。
- [x] 微信开发者工具无编译错误。

## 功能验收（本地）

- [ ] 首页加载、分类横向滚动、下拉刷新、触底分页正常。
- [ ] 搜索输入、搜索结果分页和空结果状态正常。
- [ ] 播放页加载视频、播放进度、弹幕和互动操作正常。
- [ ] 个人中心未登录状态显示微信一键登录入口。
- [ ] 调用微信一键登录后，后端返回 JWT；首次登录创建账号，再次登录复用同一账号。
- [x] 未配置小程序凭据时返回"微信登录未启用"，不影响服务启动（有单测覆盖）。

## 分享验收

- [x] 播放页已接入 `onShareAppMessage`（好友卡片）与 `onShareTimeline`（朋友圈），
      分享路径携带 `bvid`；构建产物中已确认编译进 `pages/video/video.js`。
- [ ] 好友卡片标题使用视频标题，封面使用视频封面，无封面时使用默认封面（**需真机实测**）。
- [ ] 好友卡片路径为 `/pages/video/video?bvid=<值>`，打开后能定位原视频（**需真机实测**）。
- [ ] 朋友圈分享标题和 `bvid` 查询参数正确（**需真机实测**）。
- [ ] 分享到会话后冷启动、未登录访问和已登录访问均正常（**需真机实测**）。

## 提审（当前不做）

- [ ] 完成隐私保护指引、用户协议、内容投诉/举报入口和注销/账号删除说明。
- [ ] 开发版、体验版完成功能回归后提交审核。
- [ ] 记录审核版本、提审时间、审核反馈和补件结果；通过后更新本清单与 `docs/project/roadmap.md`。

## 当前结论

已通过真实 AppID 完成**构建级验收**（AppID 落盘、页面编译、分享与登录代码确认编入产物）。
剩余项分为两类：

1. **真机实测项**：需在微信开发者工具登录真实账号后逐条走查（不是代码问题）；
2. **平台提审项**：个人主体无视频类目资质，**明确不做**。

因此 M3-MP-03 按"个人主体 + 仅本地测试"口径收口。
